# Templates

The canonical, installable templates live in
[`packages/cli/assets/templates/`](../packages/cli/assets/templates/) — the CLI
ships and installs them via `asdlc init`. This folder exists only to point there:
one source of truth, no duplicated meaning. (Yes, we took our own medicine.)
